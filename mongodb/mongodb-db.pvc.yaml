apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-db
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeName: mongodb-db
  resources:
    requests:
      storage: 10Gi
